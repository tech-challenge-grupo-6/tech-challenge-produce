/* import { AuthMiddleware } from '../../presentation/middlewares/auth-middleware';

export const isAuthenticatedMiddlewareFactory = () => {
  const isAuthenticatedMiddleware = new AuthMiddleware();

  return {
    isAuthenticatedMiddleware,
  };
}; */
